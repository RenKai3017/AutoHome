<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="100dp">
    <ImageView
        android:id="@+id/iv_reuse_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@mipmap/ic_launcher"
        android:layout_margin="10dp"/>

    <TextView
        android:id="@+id/tv_reuse_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/iv_reuse_image"
        android:layout_toRightOf="@+id/iv_reuse_image"
        android:text="title"/>

    <TextView
        android:id="@+id/tv_reuse_bbs_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/iv_reuse_image"
        android:layout_toRightOf="@+id/iv_reuse_image"
        android:text="time"/>

    <TextView
        android:id="@+id/tv_reuse_reply_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:layout_alignBottom="@+id/tv_reuse_bbs_name"
        android:text="条数"/>
</RelativeLayout>