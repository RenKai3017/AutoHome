<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="100dp">


    <ImageView
        android:id="@+id/iv_latest_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@mipmap/ic_launcher"
        android:layout_margin="10dp"/>

    <TextView
        android:id="@+id/tv_latest_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/iv_latest_image"
        android:layout_toRightOf="@+id/iv_latest_image"
        android:text="title"/>

    <TextView
        android:id="@+id/tv_latest_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/iv_latest_image"
        android:layout_toRightOf="@+id/iv_latest_image"
        android:text="time"/>

    <TextView
        android:id="@+id/tv_latest_reply_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/iv_latest_image"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:text="条数"/>

</RelativeLayout>